# CampusLink AI 优化建议清单

> 检查日期：2026-08-30
> 范围：后端（FastAPI + ChromaDB + RAG）、前端（Vue3 + Element Plus）、部署（Docker / 脚本）、工程化
> 优先级说明：🔴 P0 = 功能缺陷，建议尽快修；🟠 P1 = 性能/体验，收益明显；🟡 P2 = 工程质量；🟢 P3 = 后续路线图可做
>
> **进度跟踪**：✅ = 已完成（详见 CHANGELOG.md 对应变更记录）

---

## 总体评价

项目整体结构清晰、分层合理（api / services / database / models），注释完善，RAG 基础链路（解析 → 切分 → 向量化 → 检索 → 生成）是通的，作为 MVP 完成度不错。主要短板集中在：

1. **Docker 部署路径走不通**（前端产物没有被服务出来）
2. **同步阻塞问题**（并发下整站卡死）
3. **检索质量没有兜底**（无相似度阈值，无关内容会污染回答）
4. **工程化缺失**（无测试、无日志、无 lint）

---

## 🔴 P0 高优先级

### 1. Docker 部署后前端页面无法访问 ✅ 已完成（2026-08-30 变更一）

**位置**：`Dockerfile`、`docker-compose.yml`、`backend/app/main.py`

Dockerfile 花了一个构建阶段把前端 `dist/` 打进镜像（第 49 行），但后端从未挂载静态文件，CMD 只启动 uvicorn。结果：`docker compose up` 后访问 `http://localhost:8000` 只有 API，没有任何 UI；README 中"服务运行在 http://localhost:8000"实际达不到。

**建议**（二选一）：

方案 A：FastAPI 直接托管静态文件（单容器部署，改动最小）

```python
# main.py 末尾，生产模式下启用
from fastapi.staticfiles import StaticFiles
from fastapi.responses import FileResponse

FRONTEND_DIST = Path(__file__).resolve().parent.parent / "frontend" / "dist"
if FRONTEND_DIST.exists():
    # SPA fallback：非 /api 路径全部回退到 index.html
    @app.get("/{full_path:path}", include_in_schema=False)
    async def spa_fallback(full_path: str):
        target = FRONTEND_DIST / full_path
        if full_path and target.is_file():
            return FileResponse(target)
        return FileResponse(FRONTEND_DIST / "index.html")
```

方案 B：compose 里加 nginx 服务托管 dist 并反代 `/api`（更标准，适合后续扩展）。

### 2. 同步代码阻塞事件循环，并发时整站卡死 ✅ 已完成（2026-08-30 变更三）

**位置**：`backend/app/api/chat.py:34`、`backend/app/services/rag_service.py:15`、`backend/app/api/document.py:55`

`rag_query()` 是同步函数，内部包含两块耗时操作：
- sentence-transformers 的 `encode()`（CPU 密集）
- OpenAI SDK 同步 HTTP 调用（网络阻塞，10~30 秒）

而 `chat()` 是 `async def`，直接调用它们会把整个事件循环卡住——一个用户提问期间，其他所有请求（包括健康检查、另一个用户的上传）全部排队。批量上传接口里的 `file.file.read()` 同理。

**建议**（按改动量从小到大）：

1. 最简单：把 `async def chat()` 改成普通 `def`，FastAPI 会自动丢进线程池执行，一行改动解决主要问题；
2. 或保留 async，用 `await asyncio.get_event_loop().run_in_executor(None, rag_query, question)`；
3. LLM 调用换成 `AsyncOpenAI` + `await`。

### 3. 检索无相似度阈值，无关内容会污染回答 ✅ 已完成（2026-08-30 变更四）

**位置**：`backend/app/database/chroma_client.py:118`（`search_similar`）

当前 Top K 检索无条件返回 K 个结果，即使相似度很差也会全部拼进 Prompt。用户问一个知识库完全没有的问题时，模型可能被弱相关片段带偏。

**建议**：

```python
# search_similar 中增加阈值过滤（ChromaDB 余弦距离 = 1 - 余弦相似度）
SIMILARITY_THRESHOLD = 0.65  # 距离小于该值才认为相关，需按实际效果调参

documents = [d for d in documents if d["distance"] <= SIMILARITY_THRESHOLD]
```

配合 `rag_service` 中"过滤后为空则直接返回『暂无相关信息』"的短路逻辑（当前只在知识库整体为空时短路）。进阶可加 bge-reranker 重排、BM25+向量混合检索。

### 4. 删除文档后 uploads/ 里的原文件成为孤儿 ✅ 已完成（2026-08-30 变更三）

**位置**：`backend/app/database/chroma_client.py:150`（`delete_document`）

metadata 里存了 `original_file`，但删除文档只删 ChromaDB 的 chunks，`uploads/` 下的源文件永远留着，磁盘会无限增长。

**建议**：删除成功后按 metadata 里的 `original_file` 同步删除磁盘文件；上传接口也可考虑解析入库成功后即删除源文件（除非需要保留做索引重建）。

### 5. 上传接口无文件大小限制，文件名未消毒 ✅ 已完成（2026-08-30 变更三）

**位置**：`backend/app/api/document.py:42-61`

- `content = file.file.read()` 全量读进内存，且没有大小上限——一个大文件即可打爆内存/磁盘；
- `file.filename` 未经清洗直接拼进保存路径（`uuid_{filename}`），含 `../`、`\` 的恶意文件名有路径穿越风险。

**建议**：

```python
MAX_FILE_SIZE = 20 * 1024 * 1024  # 20MB

safe_name = Path(file.filename).name          # 去掉路径成分
extension = Path(safe_name).suffix.lower()

content = file.file.read(MAX_FILE_SIZE + 1)
if len(content) > MAX_FILE_SIZE:
    return BatchUploadFileResult(success=False, filename=safe_name, message="文件超过 20MB 限制")
```

---

## 🟠 P1 中优先级（体验 / 性能）

### 6. 无流式输出，等待体验差 ✅ 已完成（2026-08-30 变更九）

LLM 生成需要 10~30 秒，前端目前只有一个转圈动画，且 axios 超时 60 秒，网络稍慢就报错。

**建议**：后端加 `POST /api/chat/stream`（SSE / StreamingResponse，DeepSeek 支持 `stream=True`），前端用 `fetch` + ReadableStream 逐字渲染打字机效果。这是 RAG 应用体验提升最大的一项。

### 7. Element Plus 中文语言包没有生效 ✅ 已完成（2026-08-30 变更五）

**位置**：`frontend/src/main.ts:14`

```ts
app.use(ElementPlus, { locale: undefined }) // 注释写"默认使用中文"，实际默认是英文
```

**建议**：

```ts
import zhCn from 'element-plus/es/locale/lang/zh-cn'
app.use(ElementPlus, { locale: zhCn })
```

否则 `el-pagination`、`el-empty`、日期组件等内置文案都是英文。

### 8. index.html 标题和语言错误 ✅ 已完成（2026-08-30 变更五）

**位置**：`frontend/index.html`

`<title>frontend</title>`、`<html lang="en">`——浏览器标签页会显示"frontend"。改成：

```html
<html lang="zh-CN">
<title>CampusLink AI - 校园智能助手</title>
```

### 9. 批量上传的逐文件结果没有展示 ✅ 已完成（2026-08-30 变更十）

后端 `BatchUploadResponse.files` 已经返回每个文件的成功/失败明细，但前端 `KnowledgeView.vue` 的 `handleUpload` 只弹了一个汇总 message。批量导入 10 个文件时，用户看不出哪个失败、为什么失败。

**建议**：上传完成后用 `ElDialog` 或 `el-table` 展示 `files` 明细（文件名 / 结果 / chunk 数 / 失败原因），失败的文件保留在已选列表里方便重传。

### 10. 重复上传同一文件不去重 ✅ 已完成（2026-08-30 变更十一，SHA-256 查重）

同一个 PDF 传两次会生成两份独立文档，检索时同一段内容出现两次，浪费 token 也干扰排序。

**建议**：入库前计算文件 SHA-256 存入 metadata，上传时先 `collection.get(where={"file_hash": hash})` 查重，命中则提示"该文件已存在"或做覆盖更新。

### 11. 知识库列表接口把所有正文读进内存

**位置**：`backend/app/database/chroma_client.py:175-249`

`get_all_documents` / `get_all_folders` 用 `collection.get()` 默认把每个 chunk 的**完整正文**都拉回来，只是为了统计 metadata。知识库大的时候非常浪费。

**建议**：

```python
all_data = collection.get(include=["metadatas"])  # 只要 metadata
```

另外 `delete_document` 可以简化为 `collection.delete(where={"doc_id": doc_id})`，不必先 get 再按 ids 删。规模再大后（>1 万 chunk），建议引入 SQLite 存文档元数据索引表，ChromaDB 只管向量。

### 12. 文档解析遗漏 DOCX 表格 ✅ 已完成（2026-08-30 变更十一）

**位置**：`backend/app/services/document_service.py:69`（`_parse_docx`）

`doc.paragraphs` 只遍历正文段落，**表格内容全部丢失**——校园文档里费用标准、开放时间经常就是表格。

**建议**：

```python
for table in doc.tables:
    for row in table.rows:
        row_text = " | ".join(cell.text.strip() for cell in row.cells)
        if row_text.strip(" |"):
            text_parts.append(row_text)
```

### 13. 重 CPU 任务占住请求线程（批量上传） ✅ 已完成（2026-08-30 变更十，BackgroundTasks + 进度轮询）

批量上传 N 个文件时，解析 + 切分 + 向量化全部串行在同一个 HTTP 请求里完成（前端为此设了 5 分钟超时）。文件一多必然超时。

**建议**：改为 FastAPI `BackgroundTasks` 或简单任务队列：上传接口立即返回 task_id，入库在后台执行，前端轮询 `/api/document/status/{task_id}` 展示真实进度。短期方案至少把每个文件的处理丢进线程池（配合第 2 条）。

---

## 🟡 P2 工程质量

### 14. 完全没有日志体系

全部使用 `print()`（`main.py`、`config.py`、`embedding_service.py`）。建议：

- 用 `logging` 标准化（时间/级别/模块名），生产关闭 DEBUG；
- 关键链路打点：检索耗时、命中数量、LLM 首 token / 总耗时、token 用量（`response.usage`），方便后续调优 TOP_K、CHUNK_SIZE 有数据支撑；
- 全局异常处理器（`@app.exception_handler`），避免现在每个接口各自 try/except，且 `str(e)` 直接返回可能泄露内部信息。

### 15. 零测试、零 lint、零 CI ✅ 已完成（2026-08-30 变更十四，后端 34 例 + 前端 5 例 + Actions 双 job）

前后端都没有任何测试和代码检查工具。建议：

- 后端：`pytest` + `httpx.AsyncClient`（FastAPI TestClient），mock 掉 OpenAI 和 embedding 模型，覆盖：切分函数（splitter 纯函数最好测）、上传校验、检索阈值逻辑；加 `ruff` + `mypy`；
- 前端：`vitest`（优先测 store 和 splitter 类纯逻辑）+ `eslint` + `prettier`；
- GitHub Actions：push 时跑 lint + test + `pnpm build` + Docker 构建验证。

`splitter_service.py` 是纯函数、`rag_service` 依赖注入简单，是性价比最高的测试切入点。

### 16. 前端打包体积：Element Plus 与图标全量引入 ✅ 已完成（2026-08-30 变更十五，主包 -79%）

**位置**：`frontend/src/main.ts:3-26`

全量 `import ElementPlus` + 注册全部约 290 个图标组件，实际只用到 5~6 个图标。

**建议**：接入 `unplugin-vue-components` + `ElementPlusResolver` 按需引入；图标改为按需 `import { Refresh, Delete } from '@element-plus/icons-vue'`。可用 `rollup-plugin-visualizer` 对比优化前后体积（预计减半以上）。

### 17. 死代码与死依赖 ✅ 已完成（2026-08-30 变更五）

- `backend/main.py`：uv 脚手架生成的 hello world，与 `app/main.py` 同名易混淆，删除；
- `pyproject.toml` 中 `markdown>=3.10.2`：后端从未使用（前端用的是 markdown-it），删除；
- `embedding_service.py` 的 `embed_text()`：无人调用，删除或保留注明用途；
- `api/chat.py:34` 中 `retrieved_docs` 变量赋值后未使用。

### 18. README 与实际配置不一致 ✅ 已完成（2026-08-30 变更五）

### 21. 元问题落入检索兜底，兜底话术生硬 ✅ 已完成（2026-08-30 变更六）
> 用户实测发现的新问题："你可以回答哪些问题"这类元问题被阈值兜底为"请咨询学校相关部门"。已实现：元问题检索前短路返回动态自我介绍（列出知识库收录内容）；超纲问题兜底话术附带主题导航。

### 22. 文档无法移动分类、新分类无从创建 ✅ 已完成（2026-08-30 变更七）
> 用户实测发现的新问题：分类只能在上传时隐式指定（UI 提示不可发现），上传后无法整理。已实现：每行"移动"按钮 + 弹窗（可选已有分类或输入新名称自动创建），后端 `PUT /api/knowledge/move` 更新 chunk 元数据；空分类无法单独存在为派生值设计的固有约束，独立分类实体列入路线图（依赖 #11 元数据索引）。

README 写"500 字/块、Top5"，`config.py` 实际是 `CHUNK_SIZE=800 / CHUNK_OVERLAP=200 / TOP_K=7`；README 项目结构里也没有 folders 接口和批量上传说明。调参实验后记得同步文档，否则误导读者（这个项目定位是简历/作品集项目，文档一致性更重要）。

### 19. API 设计小问题

- `DELETE /api/knowledge/delete` 用请求体传 `doc_id`：DELETE 携带 body 兼容性差（部分代理/客户端会丢弃），建议改为 `DELETE /api/knowledge/{doc_id}`，旧接口保留一个版本过渡；
- 健康检查可以补充 embedding 模型加载状态、版本号、ChromaDB 状态细分；
- OpenAI 客户端建议显式设置 `timeout=60, max_retries=2`；
- CORS 白名单从环境变量读取，方便部署时配置域名。

### 20. 聊天回答缺少"参考来源" ✅ 已完成（2026-08-30 变更九）

`rag_query` 已经拿到了每个 chunk 的 metadata（文件名、chunk_index、distance），但 `ChatResponse` 只返回 answer。把来源一并返回、前端在回答底部展示"📎 参考：xxx.pdf（第 2 段）"，可信度和可调试性都会明显提升，也是答辩/面试时很好的展示点。

---

## 🟢 P3 路线图建议（对应 V2/V3）

| 方向 | 说明 |
|------|------|
| **多轮对话** | ✅ 已完成（2026-08-30 变更十六）：前端携带最近历史 + 服务端 LLM 追问改写（改写后检索）+ 历史上下文生成；服务端会话管理（跨设备续聊）仍列 V2 |
| **聊天记录持久化** | ✅ 已完成（2026-08-30 变更十七）：localStorage 持久化（上限 50 条）+ 清空按钮；顺带修复流式渲染失效的响应式漏洞。跨设备同步需 V2 服务端会话管理 |
| **检索增强** | bge-reranker-base 重排、BM25+向量混合检索、按标题路径给 chunk 注入上下文（"文档名 > 章节"前缀） |
| **上传/删除鉴权** | ✅ 已完成（2026-08-30 变更十八）：管理面接口（上传/删除/移动/列表）要求 `X-Admin-Key` 匹配 `ADMIN_KEY` 环境变量，前端验证弹窗 + 自动附带请求头；聊天问答保持公开 |
| **卷数据备份** | 数据持久化路径 bug 已修复（见 CHANGELOG 变更八），但定期备份 chroma/uploads 卷到宿主机目录仍是值得加的保险措施 |
| **扫描版 PDF** | pypdf 提不出文字的扫描件目前直接报错，可提示用户，进阶接 OCR |
| **Markdown 切分优化** | MD 文件按标题层级切分而不是当纯文本，检索粒度更准 |
| **监控** | 接入简单的请求统计（QPS、平均响应、命中率），为调参提供数据 |

---

## 建议的实施顺序

1. **第一批（半天）**：P0 #1 Docker 静态托管 → #2 `async def` 改 `def` → #7/#8 locale 和 index.html → #17 死代码清理
2. **第二批（1~2 天）**：#3 检索阈值 → #5 上传限制与消毒 → #4 孤儿文件清理 → #12 DOCX 表格 → #9 明细展示
3. **第三批（持续）**：#6 流式输出 → #14 日志 → #15 测试/CI → #16 按需引入 → P3 路线图
